# Target API Host
HOST=api.superwhisper.com

# Secret keys and tokens (replace with actual values)
X_ID=your-x-id-here
X_SIGNATURE=your-x-signature-here
X_LICENSE=your-x-license-here

# Dummy User-Agent string (replace if needed)
USER_AGENT=dummy-user-agent/1.0 (platform: example)

# Optional - Port for running the proxy server (if needed)
PORT=443
